spring:
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai_chat?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: meng050319

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.example.aichat.dto
  configuration:
    map-underscore-to-camel-case: true

mcp:
  zhipu:
    search-url: https://open.bigmodel.cn/api/mcp/web_search/sse
    api-key: b10d5aa03c3348b688c51fc00cd4dbfb.uFxrmeRwiuFGNhpe
  local:
    enabled: true
    # 启动本地 prime-mcp-server JAR 的命令（逗号分隔）
    # 先执行 cd prime-mcp-server && mvn package 生成 JAR 后，修改下面的路径
    command: "java,-jar,C:/Users/30749/Desktop/AI-Chat/prime-mcp-server/target/prime-mcp-server-1.0.0.jar"

rag:
  chunk-size: 500
  chunk-overlap: 80
  max-context-chars: 2400
  embedding-batch-size: 32

embedding:
  base-url: http://localhost:1234/v1
  model-name: text-embedding-qwen3-embedding-0.6b

voice:
  # SenseVoice 的本地或者云端 API 地址
  asr-url: http://127.0.0.1:8080/api/asr
  # GPT-SoVITS 的本地 API 地址 (默认9880)
  tts-url: http://127.0.0.1:9880/tts